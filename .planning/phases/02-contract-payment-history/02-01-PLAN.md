---
phase: 02-contract-payment-history
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/api/src/contract/contract.service.spec.ts
  - apps/api/src/payment/payment.service.spec.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - PAY-01
  - PAY-02

must_haves:
  truths:
    - "contract.service.spec.ts exists and has failing tests for tenantId filter returning contracts with department.property.name"
    - "payment.service.spec.ts exists and has failing tests for findByContract returning payments by contractId"
  artifacts:
    - path: "apps/api/src/contract/contract.service.spec.ts"
      provides: "Unit tests covering CONT-01 through CONT-04"
      exports: []
    - path: "apps/api/src/payment/payment.service.spec.ts"
      provides: "Unit tests covering PAY-01 and PAY-02"
      exports: []
  key_links:
    - from: "contract.service.spec.ts"
      to: "ContractService.findAll"
      via: "Jest mock of contractRepository.find"
      pattern: "mockContractRepository\\.find"
    - from: "payment.service.spec.ts"
      to: "PaymentService.findByContract"
      via: "Jest mock of paymentRepository.find"
      pattern: "mockPaymentRepository\\.find"
---

<objective>
Create failing unit test scaffolds for ContractService and PaymentService that define the expected behaviors for Phase 2 API changes.

Purpose: Establish test contracts before implementation so Plan 02 has clear RED->GREEN targets. The existing `contract.overlap.spec.ts` covers availability logic — these new files cover query filtering and relation loading.
Output: Two spec files with failing tests for the methods that Plan 02 will implement.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-contract-payment-history/02-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract service tests (CONT-01 through CONT-04)</name>
  <files>apps/api/src/contract/contract.service.spec.ts</files>
  <behavior>
    - Test 1: findAll() with no args calls contractRepository.find with relations including 'department.property'
    - Test 2: findAll(tenantId) calls contractRepository.find with where: { tenantId } and relations including 'department.property'
    - Test 3: findAll(undefined, departmentId) calls contractRepository.find with where: { departmentId }
    - Test 4: findAll(tenantId) result includes department.property.name in the returned contract shape
  </behavior>
  <action>
    Create apps/api/src/contract/contract.service.spec.ts. Pattern: mirror the module setup from contract.overlap.spec.ts (same three repository mocks: contractRepository, tenantRepository, departmentRepository; same TestingModule bootstrap; same jest.clearAllMocks() in beforeEach).

    Mock contractRepository.find to return a contract object with nested department.property shape:
    ```
    { id: 'c1', tenantId: 't1', rentAmount: '1500', startDate: '2026-01-01', endDate: '2026-12-31',
      department: { id: 'd1', name: 'Depto 101', property: { id: 'p1', name: 'Edificio Central', address: 'Av. Lima 123' } } }
    ```

    Write tests inside a describe('findAll', ...) block. Each test:
    1. Calls service.findAll() or service.findAll(tenantId) or service.findAll(undefined, departmentId)
    2. Expects mockContractRepository.find to have been called with an object matching { relations: expect.arrayContaining(['department.property']) } for the tenantId cases
    3. For the tenantId filter test, also expects where: { tenantId: 't1' }
    4. For the departmentId filter test, expects where: { departmentId: 'd1' }
    5. For the property name test, expects the returned contract's department.property.name to equal 'Edificio Central'

    These tests MUST fail because ContractService.findAll currently only accepts departmentId (not tenantId) and uses relations: ['tenant', 'department'] (missing 'department.property').

    Run after writing: cd apps/api && npm test -- --testPathPattern="contract.service" --passWithNoTests
    The new tests should FAIL (RED state). The overlap tests should still pass.
  </action>
  <verify>
    <automated>cd /Users/georgemaxi/project-t/apps/api && npm test -- --testPathPattern="contract.service" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>contract.service.spec.ts exists; new describe('findAll') tests are present and FAILING (RED); contract.overlap.spec.ts tests still pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing payment service tests (PAY-01 and PAY-02)</name>
  <files>apps/api/src/payment/payment.service.spec.ts</files>
  <behavior>
    - Test 1: findByContract(contractId) calls paymentRepository.find with where: { contractId } and order: { date: 'DESC' }
    - Test 2: findByContract(contractId) returns only payments matching that contractId
    - Test 3: findByContract(contractId) returns payments with amount and date fields present
  </behavior>
  <action>
    Create apps/api/src/payment/payment.service.spec.ts. Bootstrap TestingModule with two repository mocks: paymentRepository (find, findOne, save, create, remove) and contractRepository (findOne). Same pattern as contract.overlap.spec.ts.

    Mock paymentRepository.find to return:
    ```
    [
      { id: 'pay1', contractId: 'c1', amount: '500', date: '2026-02-01', type: 'rent', contract: { id: 'c1' } },
      { id: 'pay2', contractId: 'c1', amount: '120', date: '2026-01-01', type: 'water', contract: { id: 'c1' } },
    ]
    ```

    Write tests inside a describe('findByContract', ...) block. Each test:
    1. Calls service.findByContract('c1')
    2. Test 1: expects mockPaymentRepository.find called with { where: { contractId: 'c1' }, order: { date: 'DESC' } }
    2. Test 2: expects result to have length 2 and both entries have contractId 'c1'
    3. Test 3: expects each result to have 'amount' and 'date' defined

    These tests MUST fail because PaymentService has no findByContract method yet.

    Run after writing: cd apps/api && npm test -- --testPathPattern="payment.service" --passWithNoTests
    The new tests should FAIL (RED state).
  </action>
  <verify>
    <automated>cd /Users/georgemaxi/project-t/apps/api && npm test -- --testPathPattern="payment.service" --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <done>payment.service.spec.ts exists; describe('findByContract') tests are present and FAILING (RED)</done>
</task>

</tasks>

<verification>
Both spec files exist and their new tests are in RED state (failing because the methods/signatures they test do not yet exist). The existing contract.overlap.spec.ts continues to pass — no regressions.

Run: cd /Users/georgemaxi/project-t/apps/api && npm test -- --passWithNoTests 2>&1 | tail -30
</verification>

<success_criteria>
- apps/api/src/contract/contract.service.spec.ts created with describe('findAll') tests covering tenantId filter and department.property relation
- apps/api/src/payment/payment.service.spec.ts created with describe('findByContract') tests covering contractId filter and amount/date fields
- New tests FAIL (RED state confirming they test not-yet-implemented code)
- contract.overlap.spec.ts tests still PASS
</success_criteria>

<output>
After completion, create `.planning/phases/02-contract-payment-history/02-01-SUMMARY.md`
</output>
