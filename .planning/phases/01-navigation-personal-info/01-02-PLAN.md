---
phase: 01-navigation-personal-info
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/pages/Tenants.tsx
autonomous: false
requirements:
  - NAV-01

must_haves:
  truths:
    - 'Clicking a tenant row in /tenants navigates to /tenants/:id without a full page reload'
    - 'The URL changes to /tenants/:id after clicking'
    - 'The navigation uses React Router <Link> (no window.location, no full reload)'
  artifacts:
    - path: 'apps/client/src/pages/Tenants.tsx'
      provides: 'Tenant list with clickable name cells linking to detail page'
      contains: 'Link.*tenants.*t.id'
  key_links:
    - from: 'apps/client/src/pages/Tenants.tsx'
      to: 'apps/client/src/pages/TenantDashboard.tsx'
      via: 'React Router <Link to={`/tenants/${t.id}`}>'
      pattern: "to=.*tenants.*t\\.id"
---

<objective>
Make tenant list rows navigable — clicking a tenant's name cell navigates to `/tenants/:id`.

Purpose: NAV-01 requires that clicking a tenant card navigates to the detail page. Currently `Tenants.tsx` renders plain `<tr>` elements with no navigation action. The route and the detail page already exist; only the `<Link>` in the list is missing.

Output: Name cell in the tenant table becomes a `<Link>` using the already-imported `react-router-dom` Link component.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@apps/client/src/pages/Tenants.tsx

<interfaces>
<!-- Link is already imported on line 3 — no new import needed -->
From apps/client/src/pages/Tenants.tsx (line 3):
```typescript
import { Link } from 'react-router-dom';
```

<!-- Current name cell (lines 240-246) — plain div, no navigation -->

From apps/client/src/pages/Tenants.tsx (tbody row):

```tsx
<td className="px-5 py-3.5">
  <div className="flex items-center gap-3">
    <div className="w-8 h-8 rounded-full bg-gradient-to-br from-emerald-100 dark:from-emerald-900/40 to-emerald-50 dark:to-emerald-950/30 flex items-center justify-center text-emerald-700 dark:text-emerald-300 font-semibold text-xs ring-1 ring-emerald-200/50 dark:ring-emerald-700/40">
      {t.name.charAt(0).toUpperCase()}
    </div>
    <span className="font-medium text-on-surface">{t.name}</span>
  </div>
</td>
```

<!-- Route already exists in App.tsx — DO NOT MODIFY App.tsx -->

Route: tenants/:id → TenantDashboard (inside ProtectedRoute, confirmed existing)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap tenant name cell with Link</name>
  <files>apps/client/src/pages/Tenants.tsx</files>
  <action>
    In the `<tbody>` map (around line 238), find the name `<td>` that contains the avatar div and tenant name span. Replace the wrapping `<div className="flex items-center gap-3">` with a `<Link>` that navigates to `/tenants/${t.id}`. Preserve all existing classes and child elements exactly:

    ```tsx
    <td className="px-5 py-3.5">
      <Link to={`/tenants/${t.id}`} className="flex items-center gap-3 hover:underline">
        <div className="w-8 h-8 rounded-full bg-gradient-to-br from-emerald-100 dark:from-emerald-900/40 to-emerald-50 dark:to-emerald-950/30 flex items-center justify-center text-emerald-700 dark:text-emerald-300 font-semibold text-xs ring-1 ring-emerald-200/50 dark:ring-emerald-700/40">
          {t.name.charAt(0).toUpperCase()}
        </div>
        <span className="font-medium text-on-surface">{t.name}</span>
      </Link>
    </td>
    ```

    Rules:
    - Replace only the `<div className="flex items-center gap-3">` with `<Link to={...} className="flex items-center gap-3 hover:underline">` — swap tag only, keep all children intact
    - Do NOT make the entire `<tr>` a link (invalid HTML)
    - Do NOT add `useNavigate` or `onClick` — `<Link>` is sufficient and consistent with existing usage
    - Do NOT modify the pre-existing bug on line 75 (`Number(selectedPropertyId)` UUID comparison) — out of scope
    - `Link` is already imported (line 3) — no import change needed

  </action>
  <verify>
    <automated>cd /Users/georgemaxi/project-t && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>TypeScript compilation passes. The name cell `<td>` contains a `<Link to={'/tenants/${t.id}'}>` wrapping the avatar and name span.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify tenant navigation works in browser</name>
  <files>apps/client/src/pages/Tenants.tsx</files>
  <action>
    Human verification required. Start the app with `npm run dev` from the project root and test the following:

    1. Navigate to http://localhost:5173/tenants
    2. Click on any tenant's name in the list
    3. Confirm: URL changes to `/tenants/:id` (e.g. `/tenants/some-uuid`) without a full page reload
    4. Confirm: The detail page loads showing the tenant's name, email, and phone
    5. Confirm: Browser back button returns to `/tenants` without a full reload
    6. Navigate directly to a known `/tenants/:id` URL — confirm page loads correctly

  </action>
  <verify>
    <automated>MISSING — navigation behavior requires manual browser verification</automated>
  </verify>
  <done>User confirms: clicking a tenant name navigates to the detail page via SPA routing (no full reload), and direct URL access works correctly.</done>
  <resume-signal>Type "approved" if navigation works correctly, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
1. `cd /Users/georgemaxi/project-t && npx tsc --noEmit` — zero TypeScript errors
2. Manual: Click tenant name in /tenants — lands on /tenants/:id, URL updates, no full page reload
3. Manual: Back button from /tenants/:id returns to /tenants
4. Manual: Direct URL `/tenants/:id` loads correct tenant data
</verification>

<success_criteria>

- Clicking a tenant name in `/tenants` navigates to `/tenants/:id` via React Router (SPA navigation, no reload)
- The `<Link>` wraps only the name cell, not the entire row
- TypeScript compilation is clean
- Human verification approved
  </success_criteria>

<output>
After completion, create `.planning/phases/01-navigation-personal-info/01-02-SUMMARY.md` following the summary template.
</output>
